// ROME-TAG: 0xRFC0DE
import { runRomeFix } from '../utils/rome.js';
import { runAutoRefactor } from '../utils/refactor.js';
import logger from '../utils/logger.js';
export default async function runFix() {
    logger.info('🚀 QFLUSH — ROME AUTO FIX STARTED');
    try {
        await runRomeFix();
        await runAutoRefactor();
        logger.success('✨ Syntax OK — Complexity OK — Codebase Cleaned');
    }
    catch (e) {
        logger.warn('❌ qflush fix failed: ' + String(e));
    }
}
